<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeroone.star.course.mapper.CourseSectionMapper">

    <select id="getCourseSections" resultType="com.zeroone.star.course.entity.CourseSectionDO">
        SELECT id, course_id, title, lesson_count, creator, editor, add_time, edit_time, deleted
        FROM course_section
        WHERE deleted = 0
          AND course_id = #{courseId}
          <if test="chapterName != null">
            AND title LIKE CONCAT('%', #{chapterName}, '%')
          </if>
        ORDER BY id ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countCourseSections" resultType="java.lang.Long">
        SELECT COUNT(id)
        FROM course_section
        WHERE deleted = 0
          AND course_id = #{courseId}
          <if test="chapterName != null">
            AND title LIKE CONCAT('%', #{chapterName}, '%')
          </if>
    </select>

</mapper>